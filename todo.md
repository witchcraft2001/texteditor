# TODO

1. Сделать корректное определение кода конца строки `CR` / `LF` / `CR+LF` при загрузке файла, так как сейчас некоторые файлы загружаются некорректно.
2. При конкатенации длинных строк, которые превышают размер буфера, выдавать пользователю сообщение. Сейчас строка молча обрезается.
3. При открытии файла, у которого строки длиннее 255 символов, выдавать предупреждение пользователю, что корректная работа с таким файлом невозможна, и помечать его как read-only. Предлагать пользователю принудительно расставить переносы с ограничением длины строки.
